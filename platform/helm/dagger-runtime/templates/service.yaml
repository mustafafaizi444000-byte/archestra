{{- if .Values.service.create }}
{{- if not .Values.dagger.engine.port }}
{{- fail "service.create=true requires dagger.engine.port to enable the Dagger TCP listener" }}
{{- end }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "archestra-dagger-runtime.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "archestra-dagger-runtime.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "archestra-dagger-runtime.engineSelectorLabels" . | nindent 4 }}
  ports:
    - name: dagger
      port: {{ .Values.service.port }}
      targetPort: dagger
      protocol: TCP
{{- end }}
